---
import { cn } from '@/utils'

const { class: className, title } = Astro.props
---

<section class={cn(className, 'flex flex-col gap-y-5')}>
	<div class='text-xl font-semibold'>
		<h2>{title}</h2>
	</div>
	<div class='mx-auto flex max-w-3xl flex-col gap-y-3'>
		<slot />
	</div>
</section>
